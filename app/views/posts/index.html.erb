<h1>Welcome to blog</h1>

<% @post.each do |post|%>
	<div class = "post_wrapper">
		<h2 class="title"><%= link_to post.title,post %></h2>
		<p class="date"><%= post.created_at.strftime("%p: %B, %d, %Y") %></p>
	</div>
<% end %>